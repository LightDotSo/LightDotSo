{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "cache": false,
      "inputs": [
        "src/**",
        "../gradle/**",
        "../build.gradle",
        "../gradlew",
        "../gradle.properties",
        "../settings.gradle"
      ],
      "outputs": ["build/**"]
    }
  }
}
