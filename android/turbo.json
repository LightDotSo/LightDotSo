{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "cache": false,
      "inputs": [
        "../gradle/**",
        "../build.gradle",
        "../gradle.properties",
        "../gradlew",
        "../gradlew.bat",
        "../Makefile",
        "../settings.gradle",
        "./build.gradle",
        "./proguard-rules.pro",
        "src/**"
      ],
      "outputs": ["build/**"]
    }
  }
}
