# API

## Overview

Light's API is designed to run with the latest state of the underlying chains.
The below gives a brief overview of the API.

## Rate Limiting

The API is limited to 100 requests per minute per IP address.
If you exceed this limit, you will receive a 429 status code.
