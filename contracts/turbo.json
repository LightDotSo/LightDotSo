{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "cache": false,
      "inputs": ["src/**", "test/**", "../lib/**", "../.gitmodules", "../hardhat.config.js", "../foundry.toml", "../remappings.txt"],
      "outputs": ["../out", "../.gas-snapshot", "../foundry.toml"]
    },
    "lint": {
      "dependsOn": ["build"]
    }
  }
}
