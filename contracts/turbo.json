{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "inputs": ["src/**", "test/**", "../lib/**", "../.gitmodules", "../foundry.toml", "../remappings.txt"],
      "outputs": ["../out", "../.gas-snapshot", "../foundry.toml"]
    },
    "lint": {
      "dependsOn": ["build"]
    }
  }
}
