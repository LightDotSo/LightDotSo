{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "cache": false,
      "inputs": [
        "**",
        "../.cargo/**",
        "../crates/**",
        "../Cargo.lock",
        "../Cargo.toml",
        "../rust-toolchain.toml",
        "../rustfmt.toml"
      ]
    }
  }
}
