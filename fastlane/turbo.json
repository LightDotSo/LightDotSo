{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "cache": false,
      "dependsOn": ["^build"],
      "inputs": [
        "**",
        "../Gemfile",
        "../Gemfile.lock"
      ]
    }
  }
}
