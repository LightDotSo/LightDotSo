# fly.toml app configuration file generated for lightdotso-bundler-sepolia on 2023-08-20T13:21:53-07:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = "lightdotso-bundler-sepolia"
primary_region = "sjc"
kill_signal = "SIGINT"
kill_timeout = "5s"

[experimental]
  entrypoint = ["/usr/local/bin/bundler"]

[build]
  image = "ghcr.io/lightdotso/lightdotso:latest"

[http_service]
  internal_port = 3009
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 1
  [http_service.concurrency]
    type = "requests"
    hard_limit = 250
    soft_limit = 200

[checks]
  [checks.health]
    port = 3009
    type = "http"
    interval = "15s"
    timeout = "10s"
    grace_period = "30s"
    method = "get"
    path = "/health"

[metrics]
  port = 3009
  path = "/metrics"
