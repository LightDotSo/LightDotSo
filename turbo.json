{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["!turbo.json", "!package.json", "!pnpm-lock.yaml"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"]
    },
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    },
    "//#ci": {
      "cache": false
    }
  }
}
